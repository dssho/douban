<searchbar isnavigator="{{true}}"></searchbar>
<view class="container">
  <itemview wx:for="{{items}}"
    wx:key="index" item="{{item}}" 
    itemurl="/pages/detail/detail?type={{type}}&id={{item.id}}">
  </itemview>  
</view>